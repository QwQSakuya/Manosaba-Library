# 大魔女图书馆 · 二创素材 × 数据可视化 SaaS 风格宣传动画计划

> 参考：《Introducing Linear Agent》——极简暗黑、UI 驱动、无旁白、Teaser 节奏。
> 内容配比：**60% 二创素材 / 人物立绘 / 自定义立绘生成，40% 剧情数据可视化、可检索、可浏览**。
> 目标：用 GSAP 或 Remotion，把“大魔女图书馆”包装成一支约 50–55 秒的现代产品宣传片。

---

## 1. 项目现状与可展示的真实功能

大魔女图书馆是一个《魔法少女的魔女审判》粉丝资料站，兼具两个核心价值：

1. **二创素材库**：提供 CG、角色立绘、证物、音频、原始素材索引；尤其是「立绘工坊」可以自由组合角色部件，生成并下载自定义立绘。
2. **剧情数据可视化**：把 400+ 剧情节点变成可缩放、可平移、可搜索、可按路线过滤的图谱；记录、证物、语音等资料互相关联。

它不是传统 SaaS，但非常适合用 SaaS 产品预告片的语法来展示“创作工作台”与“数据浏览器”。

| 实际页面 | 真实功能 | 可提炼为“产品卖点” |
|---|---|---|
| `gallery.html#workshop` / `workshop.html` | **立绘工坊**：选择角色 → 切换眼睛/嘴巴/脸颊/头发/身体/手脚/特效/配饰 → 实时 Canvas 合成 → 下载 PNG | “自由组合角色部件，生成你的自定义立绘” |
| `gallery.html` | CG 画廊、灯箱原图、立绘工坊入口 | “视觉资产沉浸式展厅” |
| `archive.html` | 全素材库索引：按分类/文件名搜索、分页、下载原始素材 | “所有二创素材一个入口” |
| `evidence.html` | 证物图鉴、按章节浏览、详情与关联节点 | “每件证物都连接到剧情证据链” |
| `audio.html` | 语音/音乐/SFX 筛选与试听、迷你播放器 | “声音档案即点即听” |
| `act01.html` / `act02.html` | 剧情节点图谱：缩放/平移/搜索/路线过滤/Trial 标注/章节导航 | “把 400+ 剧情节点变成一张可探索的地图” |
| `records.html` | 世界观术语、监牢规定词条、分类筛选、关联跳转 | “世界观资料自动归档、可检索” |
| `index.html` | 落地页：一周目/二周目入口、档案区、镜像站 | “从入口选择你的路线” |
| `credits.html` / `意见箱` | 制作名单、GitHub 反馈 | “社区协作与透明来源” |

已有技术基础：
- 全站默认暗色主题，已有魔法阵、蝴蝶、哥特/衬线字体等强视觉符号。
- 已集成 GSAP 3 + ScrollTrigger + SplitText + DrawSVGPlugin，且已有大量高级动效。
- 立绘工坊使用 `data/chara/index.json` + 各角色 `manifest.json` + Canvas 合成，部件数据真实可读取。
- 数据全部为静态 JSON，适合动画直接读取真实内容（节点标题、证物名、CG 图、音频标签、立绘部件）。

---

## 2. 动画定位

不是“把网站录屏”，而是用 SaaS 产品预告片的语法重新演绎网站：

- **暗黑极简**：保留现有暗色基底，减少装饰，突出发光文字与 UI 卡片。
- **创作优先**：60% 的镜头聚焦“二创素材 + 立绘工坊 + 自定义立绘生成”，让观众看到素材可以被取用、组合、导出。
- **数据能力**：40% 的镜头展示“剧情数据可视化、可检索、可浏览”，突出图谱、搜索、关联。
- **无旁白**：纯视觉 + 环境音/电子节奏，结尾一句 Slogan 收束。
- **Teaser 节奏**：约 50 秒（可扩展至 55 秒），从“打开素材库”到“生成一张自定义立绘”再到“探索整部剧情图谱”。

已确认 Slogan：
- `取素材，组立绘，探剧情。`

## 2.5 已确认决策（Grilling 结果）

| 决策点 | 确认结果 |
|---|---|
| 交付物 | GSAP 网页原型 + Remotion 成片（两者都要） |
| 时长 | 目标 50 秒，允许扩展到 55 秒 |
| 视觉风格 | 暗黑极简为底，保留魔法阵/蝴蝶/角色色点缀 |
| 素材使用 | 立绘工坊使用真实角色部件，其余场景用 UI 卡片/线框 |
| 语言 | 纯中文 |
| 内容配比 | 60% 二创素材/立绘工坊，40% 数据可视化/检索/浏览 |
| GSAP 存放 | 当前仓库内 `promo-standalone/`，不修改现有页面、不加任何入口/链接 |
| 数据快照 | 所需 JSON/图片复制进独立目录，离线可运行 |
| 立绘演示角色 | 艾玛主演示 + 希罗快速切换 |
| 播放方式 | 自动播放 + 重播/暂停/跳过（由我决定，适配 B 站对照） |
| 结尾 CTA | URL + “进入绘像展厅/立绘工坊”按钮，可点击跳转线上 `gallery.html#workshop` |
| Slogan | `取素材，组立绘，探剧情。` |
| Remotion 规格 | 16:9 1080p / H.264 / AAC |
| Remotion 工程位置 | 等 GSAP 原型确认后再定 |
| 配乐 | 免版权/CC0 曲库；管风琴为主、钢琴过渡，具体场景由我调配 |
| 品牌水印 | 只在结尾 Slogan 时出现 |
| 原型范围 | 先做 `promo-standalone/` GSAP 原型，确认风格后再建 Remotion |
| 交互工坊 | 第一阶段只做展示动画，后续再加可试玩立绘工坊 |
| B 站发布清单 | 暂只包含视频规格，发布运营后续再补 |



---

## 3. 分镜草案（50 秒，60/40 配比）

> 主版本按 50 秒编排；如内容需要可扩展至 55 秒（保留 Linear 参考节奏）。

| 时间 | 场景 | 视觉内容 | 对应真实功能 |
|---|---|---|---|
| 00:00–00:05 | 开场 Logo | 暗场中魔法阵/蝴蝶浮现，`nnk_box.webp` 从模糊到清晰，标题逐字显现 | 首页品牌 |
| 00:05–00:10 | 素材总览 | 素材分类网格自动陈列：CG / 立绘 / 证物 / 音频 / 全素材库，快速扫过 | gallery + archive |
| 00:10–00:20 | **立绘工坊 · 组合** | 艾玛主演示 → 选择角色 → 部件面板弹出，自动光标切换眼睛、嘴巴、头发、配饰，画布实时合成 | gallery#workshop |
| 00:20–00:25 | **立绘工坊 · 导出** | 点击「下载 PNG」，一张自定义立绘从画布中飞出、放大展示；快速切换到希罗 | workshop 下载功能 |
| 00:25–00:30 | 素材延伸 | 画廊灯箱自动翻页、证物卡片浮现、音频播放器波形/唱片旋转 | gallery + evidence + audio |
| 00:30–00:38 | 图谱检索 | 节点图谱自动缩放平移，搜索框输入“魔女化”，高亮相关节点与连线 | 节点图谱 + 搜索 |
| 00:38–00:45 | 关联浏览 | 词条卡片与证物/节点连线，路线过滤切换，展示“可检索、可浏览” | records + evidence + 图谱 |
| 00:45–00:50 | Slogan 收束 | 最后一张立绘缩小到图谱中央，Slogan `取素材，组立绘，探剧情。` 与 URL `manosaba-library.com` 淡入 | 品牌收束 |

> 可调整为 30s 版本：压缩 00:05–00:10 与 00:25–00:30，保留立绘工坊完整组合/导出过程。

---

## 4. 视觉与动效规范

- **背景**：`#0b0b0f` 附近深色，保留微弱魔法阵/颗粒噪点。
- **强调色**：沿用现有主题色——艾玛粉、希罗红、诺亚水蓝，作为不同功能模块的“高亮色”。
- **字体**：标题用衬线/展示字体（如 Cormorant Garamond + Noto Serif SC），数据/UI 用等宽或简洁无衬线。
- **动效语言**：
  - 镜头：缓慢推近/平移/缩放，模拟“产品演示运镜”。
  - 自动光标：模拟选择角色、切换部件、点击下载、输入搜索词，强调“系统自动完成创作流程”。
  - 立绘合成：部件从面板飞向画布、图层叠加、半透明遮罩切换，突出 Canvas 实时合成。
  - 文字：逐字/逐词 Mask 入场、数字 count-up。
  - 连线：DrawSVG 绘制节点连线，表示“检索/关联”。
  - 卡片：clip-path 揭开、位移淡入、轻微 3D tilt。
- **节奏**：立绘工坊段落稍慢（让观众看清组合过程），图谱段落稍快（体现检索效率）；关键动作使用 `power3.out` / 自定义 `witchOut` / `veilInOut`。

---

## 5. 技术方案：GSAP vs Remotion

| 维度 | GSAP（Web 动画） | Remotion（React 视频） |
|---|---|---|
| 现有基础 | 项目已集成 GSAP + 插件，零新依赖 | 需要新建 Node/React 工程，引入 Remotion |
| 交付形态 | 网页内自动播放/滚动触发的交互式 Teaser | 可渲染成 MP4，发布到视频平台 |
| 精确帧控制 | 依赖 timeline 时间，适合网页实时播放 | 按帧渲染，适合 50–55s 成片 |
| 与真实数据结合 | 可直接 fetch 现有 JSON 动态渲染 | 也可读 JSON，但需打包数据 |
| 立绘工坊表现 | 可调用真实 Canvas 合成，甚至让用户交互 | 更适合预渲染好的部件图/合成结果 |
| 交互性 | 强：可暂停、点击跳转、响应滚动 | 弱：本质是视频 |
| 适合场景 | 独立 `promo-standalone/` 网页 | 宣传片视频、B站/YouTube 投放 |

### 建议

- **网页版**：用 **GSAP**，在 `promo-standalone/` 内独立实现，不链接现有页面；立绘工坊段落直接使用快照后的真实部件数据做动画。
- **成片**：用 **Remotion**，在 GSAP 原型确认后再决定工程位置；按 16:9 1080p / H.264 / AAC 导出 MP4，适配 B 站。
- **折中路线**：先用 GSAP 做网页版交互原型验证风格；确认后再用 Remotion 复刻成片。

---

## 6. 实施计划

### Phase 0 — 风格确认（Grill 清单）
✅ 已完成，确认结果见上文「2.5 已确认决策」。

### Phase 1 — 原型（GSAP 优先）
- 在仓库内新建独立目录 `promo-standalone/`，**不修改现有页面、不加入口、不产生任何页面链接**。
- 目录结构建议：
  ```
  promo-standalone/
  ├── index.html          # 独立宣传动画页
  ├── assets/
  │   ├── css/promo.css
  │   ├── js/promo.js
  │   ├── lib/            # 可复用当前项目的 GSAP 及插件
  │   └── chara/          # 艾玛/希罗所需部件图快照（从 R2 或本地数据复制）
  └── data/
      ├── nodes-demo.json # 从 act01/act02 提取的少量节点子集
      ├── evidence-demo.json
      ├── records-demo.json
      ├── gallery-demo.json
      └── audio-demo.json
  ```
- 用真实 JSON 数据生成 UI 卡片（节点、证物、词条、画廊、音频、素材分类）。
- 读取快照后的 `chara/index.json` + 艾玛/希罗 `manifest.json`，用真实部件图做立绘工坊动画。
- 实现核心 timeline：开场 → 素材总览 → 立绘组合 → 导出 → 素材延伸 → 图谱检索 → 关联浏览 → Slogan。
- 加入自动光标、数字滚动、DrawSVG 连线、卡片入场、Canvas 合成视觉。
- 播放控制：自动播放 + 重播/暂停/跳过；播放结束后停在结尾画面并显示“重播”。

### Phase 2 — 打磨
- 统一缓动与节奏，加入背景粒子/魔法阵细节。
- 支持 `prefers-reduced-motion` 降级。
- 移动端适配（若网页版）。

### Phase 3 — 成片（Remotion，待 GSAP 原型确认后再定位置）
- 在 GSAP 原型确认后，再决定 `remotion/` 工程放在仓库内还是仓库外。
- 把 GSAP timeline 翻译为 React 组件。
- 为立绘工坊预渲染 3–5 张“组合过程关键帧”，保证视频流畅。
- 使用 `@remotion/transitions` 做转场，导出 **16:9 1080p / H.264 / AAC MP4**（适配 B 站）。
- 合成免版权管风琴/钢琴配乐与音效，输出最终成片。

### 实施进度（2026-08-16）

- ✅ 已创建 `promo-standalone/` 独立目录，未改动现有页面、无任何入口链接。
- ✅ 已快照 GSAP 3 + ScrollTrigger + CustomEase + SplitText + DrawSVGPlugin 到 `assets/lib/`。
- ✅ 已从 R2 快照 **艾玛（126 部件，11.6MB）** 与 **希罗（113 部件，9.2MB）** 全部立绘部件及 manifest。
- ✅ 已生成演示数据：`nodes-demo.json`（搜索“魔女化”15 节点）、`records-demo.json`、`evidence-demo.json`、`gallery-demo.json`、`audio-demo.json`、`chara-demo.json`，并内嵌为 `assets/js/demo-data.js`（file:// 也可运行）。
- ✅ 已按 grill-me 结果压缩为 **40 秒** 原型：0–4 开幕 / 4–8 素材总览 / 8–17 立绘工坊（提速）/ 17–23 证物+画廊 macOS 双窗口 / 23–36 图谱检索 / 36–40 Slogan。
- ✅ 运镜重做：删除无意义旋转/漂移与开幕/结尾推近；转场前镜头朝指针方向；工坊左侧操作时聚焦面板；图谱“输入 → 向下拉开 → 命中节点推入”；证物/画廊双窗口同时滚动并带惯性。
- ✅ 证物+画廊窗口：左侧真实证物页（12 件 + 真实缩略图），右侧场景背景 `Background_001_001` 起 6 张 CG。
- ✅ 图谱不再显示 `24 节点命中` 与 `normal · bad04 · bad05`。
- ✅ 已修复：艾玛演示不切换 Head02；导出 PNG 改为小卡滑出；搜索文字使用 TsukushiMincho + 蓝色辉光专属样式。
- ✅ 已用 headless Chrome 验证：无 JS 报错、无 4xx/5xx 资源、立绘 Canvas 合成正常、双窗口滚动与时间轴推进正常。
- 📄 运镜分镜表：`CAMERA_PLAN_V3.md`。
- ⏭ 待你确认视觉/节奏后进入 Phase 3（Remotion 成片 + 配乐筛选）。



---

## 7. 风险与注意事项

- **素材版权**：CG/音频/角色素材版权归原作方，动画若公开传播需注意非商用/署名规则；建议 UI 线框优先，实机 CG/立绘谨慎使用。
- **配乐版权**：不使用游戏原声；从 Pixabay / Free PD Music / Musopen 等免版权或公有领域曲库筛选管风琴/钢琴配乐，并在 credits 中记录来源。
- **立绘数据来源**：`chara/index.json` 和角色 `manifest.json` 可能走本地或 R2 远程线路，独立版需提前把艾玛/希罗所需部件图快照到 `promo-standalone/assets/chara/`，避免运行时依赖线上。
- **数据体积**：`act01.json` / `act02.json` 较大，独立版只提取演示所需节点子集。
- **性能**：GSAP 动画避免同时操作大量 DOM；用快照/静态卡片代替真实图谱全量渲染。
- **可访问性**：自动播放动画需提供暂停/跳过；尊重 `prefers-reduced-motion`。
- **独立性**：`promo-standalone/` 不修改现有页面、不加导航入口，也不依赖当前页面资源；若后续要发布，可单独部署该子目录。

---

## 8. 下一步

1. ✅ Grilling 已完成，已确认关键决策（见 2.5）。
2. 开始搭建 `promo-standalone/` GSAP 原型：
   - 创建独立目录与页面骨架；
   - 快照艾玛/希罗立绘部件和演示 JSON；
   - 实现 50 秒自动播放 timeline + 重播/暂停/跳过。
3. 原型确认后，再决定 Remotion 工程位置并出 B 站 16:9 1080p 成片。
