# 运镜分镜表 v3（已确认 · 已实施）

> 经 grill-me 两轮确认后重做。总时长压缩为 **40 秒**。
> 运镜原则：**镜头只解释“看什么”**，不做持续旋转/漂移；转场前镜头指向指针方向。

---

## 已确认决策

| 问题 | 决策 |
|---|---|
| Q9 转场前运镜 | ✅ 转场前镜头朝指针/下一目标方向移动 |
| Q10 双窗口滚动 | ✅ 左右窗口**同时**滚动 |
| Q11 画廊内容 | ✅ 场景背景 `Background_001_001` 起，共 6 张 |
| Q12 电影感范围 | ✅ 全部场景允许，但仅用于有意义镜头 |
| Q13 时长 | ✅ 压缩，采用 40s：0–4 / 4–8 / 8–17 / 17–23 / 23–36 / 36–40 |

---

## 40s 分镜

| 时间 | 场景 | 镜头 |
|---|---|---|
| 0–4 | 开幕 | 完全静止，只做 logo / 标题 / 细线入场；无推近 |
| 4–8 | 素材总览 | 静态卡片入场；转场前镜头朝指针方向（下一目标）移动 |
| 8–17 | 立绘工坊 | 进入时轻微 settle；每次左侧操作镜头聚焦面板（x -34 / scale 1.06）；下载回中；整体提速 |
| 17–23 | 证物 + 画廊 macOS 双窗口 | 窗口 0.94→1.0 入场；左右内容同时滚动两次（第一次滑动 → 停顿 → 第二次惯性滑动）；转场前镜头朝搜索框方向 |
| 23–36 | 图谱检索 | 先展示输入「魔女化」→ 镜头向下并拉开（scale 1.16→0.99, y 0→74）→ 节点/连线出现 → 向命中节点簇缓慢推入；**不再显示 `24节点命中` 与 `normal / bad04 / bad05`** |
| 36–40 | 结尾 | 完全静止，Slogan / URL / CTA 依次淡入；无前推 |

---

## 已实施的镜头函数

- `focusLeftPanel(at, dur)`：工坊左侧操作时聚焦面板
- `focusNeutral(at, dur)`：下载/中性位回中
- `followPointer(scene, target, at, dur)`：转场前镜头朝指针目标方向移动（±46px 限幅）

---

## 场景 5 结构

- 左侧 macOS 窗口：真实证物页（12 件证物列表 + 真实证物缩略图）
- 右侧 macOS 窗口：绘像展厅 CG 页（6 张场景背景：`Background_001_001` → `Background_005_001`）
- 两边同时滚动：`y -110` → 停顿 → `y -190`（带惯性缓动），滚动条同步
